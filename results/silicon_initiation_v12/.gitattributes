# Numerical artifacts and source snapshots are bound to exact generated bytes.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
# Preserve pytest's trailing blank line in the recorded stdout evidence.
implementation_tests.txt whitespace=blank-at-eol,space-before-tab,cr-at-eol,-blank-at-eof
