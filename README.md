# numerical-fem

Numerical realization, FEM coupling, simulation UI logic, verification, and generated results.

Owned here:
- `simulations/`
- `fem1d/`
- `tests/`
- `results/`
- `examples/`
- numerical/FEM-specific `docs/`
- numerical Python requirements

Not owned here: theory source, manuscript sources, desktop packaging, or fatigue-tester hardware/firmware.

This branch consumes validated equations from `theory-core`. Full cross-module execution is validated in `integration`.
