\documentclass[11pt]{article}
\usepackage[a4paper,margin=25mm]{geometry}
\usepackage{amsmath,amssymb,booktabs,graphicx,hyperref}
\title{One-Dimensional Fatigue Modeling of Pure Aluminum:\\Deterministic Calibration and a Probabilistic Spacing Hypothesis}
\author{Donghyeok Kim \and Minsoo Kang \and Junhyeok Park}
\date{September 2026}
\newcommand{\dd}{\mathrm d}
\begin{document}\maketitle
\begin{abstract}
We derive the exact generalized Lennard--Jones energy of an infinite uniform
one-dimensional chain, distinguish it from the cross-gap energy of two half
chains, and formulate a controlled metastable spacing-density dynamics.
Finite-rate probability transport produces phase-lag hysteresis; irreversible
crack initiation requires probability escape. Results without a calibrated
representative area and mobility are dimensionless demonstrations, not an
aluminum fatigue-life prediction. A separate optional one-registry branch uses
an exact Poisson--Bessel lattice energy and an unwrapped scalar slip density to
test whether lattice-well population remains shifted after unloading. It
demonstrates ideal reduced plasticity without claiming quantitative aluminum
crystal plasticity.
\end{abstract}

\section{Problem definition and scope}
The primary material theory describes pure single-crystal aluminum under scalar
normal tension using spacing $a$ and density $P(a,t)$. A separate optional
active mechanism uses one scalar registry $s$ for one declared slip system.
It remains a one-index row lattice and a one-slip-coordinate model; it is not a
Rubin chain, multiaxial fatigue law, or conventional crystal-plasticity model.
Two- or three-dimensional meshes may display geometry and scalar axial stress
but do not change the constitutive theory, and the registry branch is not yet
coupled to FEM. Every run declares a cubic loading direction $[hkl]$. If cubic
constants are supplied, the scalar directional modulus is projected:
\begin{equation}
E_{[hkl]}^{-1}=S_{11}-2(S_{11}-S_{12}-S_{44}/2)
(l_1^2l_2^2+l_2^2l_3^2+l_3^2l_1^2).
\end{equation}
The appearance of $S_{44}$ in this anisotropic elastic projection does not
introduce shear loading or a shear-fatigue criterion.

\section{Finite-chain pair energy and bidirectional counting}
For
$v(r)=\varepsilon_{\rm LJ}[(\sigma_{\rm LJ}/r)^m-(\sigma_{\rm LJ}/r)^n]$,
$m>n>1$, separation $ka$ occurs $N-k$ times, hence
\begin{equation}U_N(a)=\sum_{1\le i<j\le N}v(|i-j|a)
=\sum_{k=1}^{N-1}(N-k)v(ka).\end{equation}
An interior atom has two directions, while the factor $1/2$ removes pair
duplication. The usual 12--6 convention $4\epsilon[(\sigma/r)^{12}-(\sigma/r)^6]$
is separate; only for that special form is $\varepsilon_{\rm LJ}=4\epsilon$.

\section{Exact Riemann-zeta thermodynamic limit}
Using $\zeta(s)=\sum_{k\ge1}k^{-s}$,
\begin{equation}
U_\infty=\lim_{N\to\infty}{U_N\over N}
={1\over2}\sum_{k\ne0}v(|k|a)=\sum_{k\ge1}v(ka)
=\varepsilon_{\rm LJ}\left[\zeta(m)(\sigma_{\rm LJ}/a)^m
-\zeta(n)(\sigma_{\rm LJ}/a)^n\right].
\end{equation}
This is energy per atom or representative cell, not total chain energy.

\section{Homogeneous spacing energy versus local crack-gap energy}
For half-chains of internal spacing $a_0$ separated by $g$, let $q=g/a_0$.
There are $\ell+1$ cross-gap pairs with $i+j=\ell$, and
\begin{align}
S_p(q)&=\sum_{\ell\ge0}(\ell+1)(q+\ell)^{-p}
=\zeta(p-1,q)+(1-q)\zeta(p,q),\\
U_{\rm gap}&=\varepsilon_{\rm LJ}\left[(\sigma_{\rm LJ}/a_0)^mS_m(q)
-(\sigma_{\rm LJ}/a_0)^nS_n(q)\right].
\end{align}
$U_\infty$ is exact for homogeneous dilation; $U_{\rm gap}$ is direct for one
formed local opening. The active pre-initiation coordinate is the representative
spacing of a small approximately affine atomic neighbourhood, so $U_\infty$ is
the active local-homogeneous energy. $U_{\rm gap}$ is retained only for a future
post-initiation model and the two are not mixed.

\section{Equilibrium and tangent instability}
Stationarity and zero tangent stiffness give
\begin{align}
a_0&=\sigma_{\rm LJ}\left[{m\zeta(m)\over n\zeta(n)}\right]^{1/(m-n)},\\
a_c&=\sigma_{\rm LJ}\left[{m(m+1)\zeta(m)\over n(n+1)\zeta(n)}\right]^{1/(m-n)},\\
\lambda_c&=a_c/a_0=[(m+1)/(n+1)]^{1/(m-n)}=1.1077715386\ldots
\end{align}
for $m=12.19,n=6$. With $\lambda=a/a_0$,
\begin{equation}U_\infty=E_0\phi(\lambda)+C,\quad
\phi={\lambda^{-m}\over m(m-n)}-{\lambda^{-n}\over n(m-n)},\quad
\phi'(1)=0,\quad\phi''(1)=1,
\end{equation}
where
$E_0=\varepsilon_{\rm LJ}m(m-n)\zeta(m)(\sigma_{\rm LJ}/a_0)^m
=EA_0a_0$. This proves that $\phi$ is the exactly normalized zeta-lattice
energy, not an empirical deletion of zeta factors.

\section{Statistical ensemble and normalization problem}
$F=\sigma A_0$ has units N and
$\Phi=U-F(a-a_0)$. $A_0$ is a mechanical representative area, neither a
correlation area nor a finite-element area. For tensile $F$, $\Phi\to-\infty$;
for $F=0$, $U\to0$. Thus a one-spacing Gibbs law on $(0,\infty)$ is not
normalizable. We use a declared finite intact basin and conditional Gibbs
initialization. A fixed-total-length canonical chain is an alternative but
couples multiple spacings.

\section{Langevin-to-Smoluchowski derivation}
An isothermal eliminated phonon bath, rapid velocity relaxation, constant
friction and white-noise scale separation give, by fluctuation--dissipation,
\begin{equation}\gamma\dot a=-\Phi_a+\sqrt{2\gamma k_BT}\xi,
\quad\langle\xi(t)\xi(t')\rangle=\delta(t-t').\end{equation}
The forward equation is
\begin{equation}\partial_tP=-\partial_aJ,\qquad
J=-{1\over\gamma}(\Phi_aP+k_BT P_a),\quad
M_a=\gamma^{-1},\quad D_a=k_BT/\gamma.\end{equation}
No empirical diffusion or hysteresis operator is introduced.

\section{Probability current, hysteresis and energy balance}
The maintained observables are
$\int P\dd a=1$, $\bar a=\int aP\dd a$,
$\bar U=\int[U-U(a_0)]P\dd a$, and
$w_h=\int\sigma\dot{\bar\lambda}\dd t$ with
$H_k=\oint_k\sigma\dd\bar\lambda$.
For sinusoidal loading, finite mobility makes $P$ lag the instantaneous
conditional distribution. Loading and unloading distributions differ at the
same stress. Diffusion alone is not the cause; the competition between drive
and relaxation times is. Both frozen very-fast and equilibrated quasistatic
limits have vanishing loops, with a finite-rate maximum possible.

For $\mathcal F=\int P(\Phi+k_BT\ln P)\dd a$,
\begin{equation}\dot{\mathcal F}=\int P\partial_t\Phi\dd a
-\int{J^2\over M_aP}\dd a-[\mu J]_L^R,
\quad \mu=\Phi+k_BT(\ln P+1),\end{equation}
so current dissipation is nonnegative. $H_k$ is work, not stored pair energy.

\section{Irreversible initiation, survival and hazard}
A reflecting process can become periodic without accumulating damage. For
$0<F<F_c$, $U'=F$ also has stable and unstable roots; $a_b(F)>a_c$ is an
alternative energetic barrier and merges with $a_c$ at $F_c$. This initiation
study instead defines crack initiation operationally as first loss of tangent
stability of the local homogeneous environment. The intact domain therefore
has an absorbing boundary exactly at $a_c$:
\begin{equation}S=\int_\Omega\rho\dd a,\quad P_{\rm init}=1-S,
\quad h=J_{\rm out}/S.\end{equation}
The reflecting-model mass above $a_c$ is an instantaneous tail diagnostic;
first-passage flux and cumulative initiation are distinct. No post-initiation
opening or propagation is modeled.

\section{Periodic first passage and the Floquet--Perron survival spectrum}
Suppose the normal force is periodic with period $T$, and let
$\mathcal U(t_2,t_1)$ denote the linear absorbing Smoluchowski evolution.
The one-cycle operator
\begin{equation}\mathcal K=\mathcal U(T,0)\end{equation}
is positive and sub-Markov: it maps nonnegative intact densities to
nonnegative densities without increasing their mass. The finite-volume
backward-Euler step obeys the exact telescoping balance
\begin{equation}
S^{j+1}-S^j=-\Delta t\,J_{\rm out}^{j+1}.
\end{equation}
Its Chang--Cooper diffusion matrix is an irreducible M-matrix under the stated
finite-domain conditions. Perron--Frobenius theory \cite{seneta} therefore
gives a positive principal conditional density and multiplier
\begin{equation}
\boxed{\mathcal Kq_0=rq_0,\qquad \int q_0\dd\lambda=1,
\qquad 0<r<1.}
\end{equation}
This eigenproblem is not a fitted fatigue law; it is one-period propagation of
the governing PDE. Periodic conditional limits and spectral gaps for absorbed
Markov evolution, and positive principal modes for periodic-parabolic
operators, are established in broader settings in
\cite{periodicqsd,daners,timeinhomogeneous}.

Within a cycle define
\begin{equation}
g(\theta)=\int\mathcal U(\theta,0)q_0\dd\lambda,\qquad
q(\lambda,\theta)={\mathcal U(\theta,0)q_0\over g(\theta)}.
\end{equation}
Then $g(0)=1$, $g(T)=r$, and $q(\lambda,T)=q(\lambda,0)$. Since
$\dot g=-J_{\rm out}$,
\begin{equation}
\boxed{\mathcal H_c=\int_0^T{J_{\rm out}\over g}\dd t=-\log r,
\qquad S_N=r^N}
\end{equation}
for an initial principal mode. For a generic nonnegative initial density, the
successive survival ratios converge to $r$. If the positive left mode is
normalized by $w_0^Tq_0=1$, the missing initial-state coefficient is determined,
not fitted:
\begin{equation}
w_0^T\mathcal K=rw_0^T,\qquad
S_N\sim (w_0^T\rho_0)r^N.
\end{equation}
The ratio $|r_2|/r$ sets the leading conditional transient contraction per
cycle. The conditioned density satisfies
\begin{equation}
\partial_tq=-\partial_\lambda J[q]+h(t)q,
\qquad h=J_{\rm out}[q],
\end{equation}
where $+hq$ is solely the normalization correction after conditioning on
survival, not a physical probability source.
When first passage is binned by cycle, the principal mode gives the derived
geometric law
\begin{equation}
\Pr(K_{\rm init}=k)=r^{k-1}(1-r),\qquad
\mathbb E K_{\rm init}={1\over1-r},\qquad
\operatorname{Var}K_{\rm init}={r\over(1-r)^2}.
\end{equation}
This is not an assumed Weibull or empirical S--N distribution; the
phase-resolved flux retains timing information within each cycle.

This spectrum also exposes a limitation. In the principal regime the
conditional density, mean spacing, variance, and mean pair energy repeat each
cycle. Hence the active Markov model accumulates irreversible escaped
probability, but it does not continually store dissipated loop work in the
surviving pair-energy distribution. Persistent conditional-tail growth would
require a microscopically derived slow state or memory kernel; no arbitrary
energy-storage variable is introduced here.

For the uncalibrated reduced demonstration
$\beta=2000$ and
$f(t)=0.008+0.007\sin(2\pi t/12)$, the computation gives
$r=0.9047876$, $1-r=0.0952124$, and $-\log r=0.100055$, corresponding to
$N_{50}=\log(0.5)/\log r=6.928$ cycles in the principal mode. This low cycle
count explains the earlier numerical behaviour but is not an aluminum-life
prediction: $E_0$, $A_0$, temperature, mobility time scale, and the initiation
definition have not been jointly calibrated.
In the phase-resolved principal mode the force peaks at cycle phase $0.25$,
whereas the conditional mean spacing, mean interaction energy and hazard peak
near $0.35$, and the variance near $0.40$. This directly resolves the
finite-mobility probability-transport lag responsible for the hysteretic
loading--unloading distinction; it is not attributed to diffusion alone.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{../results/figures/smoluchowski_floquet/operator_verification.png}
\caption{Independent checks of the principal one-cycle survival multiplier:
power iteration, direct cycle ratios, and geometric survival from the
principal conditional density.}
\label{fig:floquet-verification}
\end{figure}

For the 70-cell dense diagnostic, $|r_2|/r=9.12\times10^{-5}$ and the
conditioned-equilibrium initialization has the independently predicted
coefficient $w_0^T\rho_0=0.98978$. This accounts for the observed convergence
of the direct cycle ratio after essentially one transient cycle.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{../results/figures/smoluchowski_floquet/biorthogonal_survival_spectrum.png}
\caption{Right conditional mode, left survival-propensity weight, spectral
contraction and the initial-state asymptotic prefactor.}
\label{fig:biorthogonal-spectrum}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{../results/figures/smoluchowski_floquet/phase_resolved_mode.png}
\caption{Phase-resolved periodic conditional density, within-cycle survival,
moments, interaction energy and first-passage hazard.}
\label{fig:phase-mode}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{../results/figures/smoluchowski_floquet/energy_tail_escape_distinction.png}
\caption{State-function energy, reflecting instability-tail diagnostic and
absorbing first-passage hazard are reported separately and are not added to
one another.}
\label{fig:energy-tail-escape}
\end{figure}

\section{Exact one-registry energy and reduced plastic-slip extension}

Plastic slip cannot be represented by changing the normal-spacing distribution
alone. We therefore activate a separate ideal mechanism with one scalar
registry coordinate, while retaining the normal crack-initiation theory as the
primary branch. Let two parallel one-index rows have normal separation $a$,
repeat $b$, and relative registry $s$. For the coefficient convention
$v=\varepsilon_c[(\sigma_{\rm LJ}/r)^m-(\sigma_{\rm LJ}/r)^n]$, the
configuration-dependent cross-row energy per commensurate repeat is
\begin{equation}
 W(a,s)=\varepsilon_c\sum_{p\in\mathbb Z}\left[
 {\sigma_{\rm LJ}^m\over[a^2+(pb+s)^2]^{m/2}}
 -{\sigma_{\rm LJ}^n\over[a^2+(pb+s)^2]^{n/2}}
 \right].
 \label{eq:registry-direct}
\end{equation}
It is neither the divergent total energy of two infinite rows nor the
collinear energy $U_\infty$. In particular, $U_\infty$ and $W$ are not added.
With $\delta=s/b$, $\eta=a/b$, and
$\mathcal Z_\nu=\sum_{p\in\mathbb Z}[(p+\delta)^2+\eta^2]^{-\nu}$, Mellin
transformation and Poisson summation give the exact identity \cite{nistbessel}
\begin{equation}
\begin{aligned}
\mathcal Z_\nu(\delta,\eta)={\sqrt\pi\over\Gamma(\nu)}\Bigg[&
\Gamma\left(\nu-\frac12\right)\eta^{1-2\nu}\\
&+4\sum_{\ell=1}^\infty\cos(2\pi\ell\delta)
\left({\pi\ell\over\eta}\right)^{\nu-1/2}
K_{\nu-1/2}(2\pi\ell\eta)\Bigg].
\end{aligned}
\label{eq:registry-bessel}
\end{equation}
Consequently
\begin{equation}
 {W\over\varepsilon_c}=
 \left({\sigma_{\rm LJ}\over b}\right)^m\mathcal Z_{m/2}
 -\left({\sigma_{\rm LJ}\over b}\right)^n\mathcal Z_{n/2}.
 \label{eq:registry-energy}
\end{equation}
The reciprocal-mode truncation is only an exponentially convergent numerical
evaluation parameter. It is not a physical interaction cutoff. The code checks
Eq.~\eqref{eq:registry-bessel} against the direct sum and, for the slowly
convergent $q=2$ case, against an independent hyperbolic closed form.

For unit loading direction $\mathbf e$, slip-plane normal $\mathbf n$, and
in-plane slip direction $\mathbf d$, the signed resolved shear is
\begin{equation}
 \tau=\mathcal M\sigma,\qquad
 \mathcal M=(\mathbf e\cdot\mathbf n)(\mathbf e\cdot\mathbf d),
 \qquad \mathbf n\cdot\mathbf d=0.
 \label{eq:registry-schmid}
\end{equation}
The interface area $A_{\rm rep}$ in the generalized slip force is kept
distinct from normal mechanical area $A_0$, correlation area $A_c$, and FEM
element area. At prescribed $a/b$, put $u=s/b$, scale energy by
$\varepsilon_c$, time by $b^2/(M_s\varepsilon_c)$, and define
$g=\tau A_{\rm rep}b/\varepsilon_c$ and
$\beta_s=\varepsilon_c/(k_BT)$. The active unwrapped density obeys
\begin{equation}
 \partial_tp=-\partial_uj,\qquad
 j=-\left[(\partial_uw-g)p+\beta_s^{-1}\partial_up\right],
 \qquad w={W\over\varepsilon_c}.
 \label{eq:registry-smoluchowski}
\end{equation}
This equation follows from the same isothermal-bath and overdamped assumptions
as the normal Smoluchowski model. A finite-volume Chang--Cooper flux advances
the full density on an unwrapped domain; the computed edge probability is
reported so that numerical truncation cannot masquerade as hardening.

Let $\delta_0$ be the verified minimum in one period and decompose
\begin{equation}
 u=\delta_0+z+\widetilde u,\qquad z\in\mathbb Z,
 \qquad -\frac12\le\widetilde u<\frac12.
 \label{eq:unwrapped-registry}
\end{equation}
A well crossing is not automatically plastic because backward crossings are
allowed. Reduced residual plasticity is declared only if unloading and a
specified relaxation interval give
\begin{equation}
 \langle\widetilde u\rangle\longrightarrow0,qquad
 \langle z\rangle\ne0,qquad
 \gamma_p={b\over h_{\rm slip}}\langle z\rangle,qquad
 \varepsilon_p=\mathcal M\gamma_p.
 \label{eq:registry-plastic-strain}
\end{equation}
For the dimensionless case $m=12.19$, $n=6$, $a/b=1$,
$\sigma_{\rm LJ}/b=1$, and $\beta_s=20$, the ideal maximum registry force is
$1.0582731$. A subcritical pulse with peak $g=0.55$ leaves
$\langle z\rangle=0.4913473$ after unloading, while
$\langle\widetilde u\rangle=1.59\times10^{-10}$ and the maximum numerical-edge
probability is $2.93\times10^{-10}$. By contrast, six complete symmetric
zero-mean cycles leave $\langle z\rangle=0.001004$. The branch therefore
distinguishes biased residual population transfer from symmetric bath-mediated
hysteresis; it does not force a plastic increment on every cycle.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{../results/figures/registry_plasticity/active_registry_plasticity.png}
\caption{Exact Fourier--Bessel registry energy, unwrapped probability
transport, recovery of intrawell registry, residual well population, and the
contrast between a biased subcritical pulse and symmetric zero-mean cycles.}
\label{fig:registry-plasticity}
\end{figure}

This reduced branch contains no dislocation line, multiplication, forest
hardening, backstress, or multiple-slip coupling. Quantitative single-crystal
aluminum use requires a declared FCC slip system, a validated EAM or
first-principles generalized stacking-fault surface \cite{dawbaskes,lu},
atomistically determined mobility/memory, $A_{\rm rep}$ and $h_{\rm slip}$,
and a common Hamiltonian before two-way coupling to normal opening.

\section{Numerical method and verification}
Cell-centered finite volumes use Chang--Cooper exponential fitting
\cite{changcooper}, backward Euler, reflecting lower flux and selectable
reflecting or absorbing upper boundary. Tests cover pair counting, zeta
convergence, equilibrium and instability derivatives, exact normalized-energy
equivalence, Gibbs stationarity, positivity, reflecting mass, absorbing
outflux balance, phase lag, frequency limits and refinement. The periodic
tests also assemble a small cycle matrix explicitly, compare its spectral
radius with power iteration, verify $S_N=r^N$, demonstrate convergence from a
generic initial density, verify invariance under a change of cycle phase
origin, and refine both grid and timestep. Moment balances
retain $-[qJ]_L^R$; conditional moments obey
$\dot{\langle q\rangle}_c=(\dot Q_q-\langle q\rangle_c\dot S)/S$.
Nonlinear LJ drift leaves the hierarchy unclosed; no Gaussian closure is used.
Registry tests independently compare the Bessel and direct sums, differentiate
the full energy, verify its period and computed minimum, check the signed
Schmid projection, preserve nonnegative unit probability, bound the unwrapped
domain-edge mass, demonstrate residual $z$ after a biased subcritical pulse,
and reject directed drift under complete symmetric zero-mean cycles.
With cycle phase $\theta=t/T$, the fast-period expansion is
$\mathcal K=I+T\overline{\mathcal L}+O(T^2)$, so $1-r=O(T)$ while
$-\log(r)/T$ approaches a finite decay rate. Under a separated instantaneous
principal mode, the slow adiabatic limit instead approaches the cycle average
of the frozen-load principal escape rate. These limits distinguish loss per
cycle from loss per unit physical time.

\section{Limitations and unresolved physical parameters}
Unresolved physical inputs include $\varepsilon_{\rm LJ},\sigma_{\rm LJ}$
consistent with the 1D coarse graining, $A_0$, $T$, mobility, escape condition,
and correlation scales $l_c,A_c,V_c$. Physical inputs are separated from
domain, cell count, timestep, tolerance and output cadence. Consequently the
reported sweep is dimensionless and is not a calibrated aluminum lifetime.
The registry branch additionally requires an interface repeat area
$A_{\rm rep}$, slip repeat $b$, homogenization thickness $h_{\rm slip}$,
mobility $M_s$, active plane and direction, and a validated aluminum energy
surface.
The nondimensional groups make this non-identifiability explicit. Using the
loading-axis single-crystal modulus,
\begin{equation}
E_0=E_{[hkl]}A_0a_0,\qquad
{Fa_0\over E_0}={\sigma\over E_{[hkl]}},\qquad
\beta={E_{[hkl]}A_0a_0\over k_BT},\qquad
t_r={\gamma a_0\over E_{[hkl]}A_0}.
\end{equation}
The representative area cancels from reduced stress but not from thermal
coarse graining or the mobility clock. It therefore cannot be replaced by a
FEM element area, and a physical cycle count cannot be claimed before $A_0$
and $\gamma$ are independently constrained.

\section{FEM/UI extension roadmap}
Only after theory validation, a separate branch may display 1D--3D meshes,
orbit/zoom/pan, loading axis, scalar normal stress, and local density/tail/
initiation maps. Mesh elements are not independent probability samples and no
multiaxial capability is implied. The optional scalar-registry solver is not
yet connected to FEM elements or the UI; such coupling will require an
explicit slip system and signed resolved-shear channel.

\begin{thebibliography}{9}
\bibitem{nist} NIST Digital Library of Mathematical Functions, Sections 25.2
and 25.11, Riemann and Hurwitz zeta functions.
\bibitem{risken} H. Risken, \emph{The Fokker--Planck Equation}, 2nd ed., Springer, 1989.
\bibitem{gardiner} C. W. Gardiner, \emph{Handbook of Stochastic Methods}, Springer, 2004.
\bibitem{changcooper} J. S. Chang and G. Cooper, ``A practical difference
scheme for Fokker--Planck equations,'' J. Comput. Phys. 6 (1970) 1--16.
\bibitem{sg} D. L. Scharfetter and H. K. Gummel, IEEE Trans. Electron Devices
16 (1969) 64--77.
\bibitem{seneta} E. Seneta, \emph{Non-negative Matrices and Markov Chains},
2nd ed., Springer, 1981.
\bibitem{periodicqsd} N. Champagnat and D. Villemonais,
``Quasi-limiting estimates for periodic absorbed Markov chains,''
arXiv:2211.02706, 2022.
\bibitem{daners} D. Daners, ``Existence and perturbation of principal
eigenvalues for a periodic-parabolic problem,'' Electron. J. Differential
Equations, Conf. 05, pp. 51--67, 2000.
\bibitem{timeinhomogeneous} N. Champagnat and D. Villemonais,
``Uniform convergence of penalized time-inhomogeneous Markov processes,''
ESAIM Probab. Stat. 22 (2018), 129--162,
doi:10.1051/ps/2017022.
\bibitem{nistbessel} NIST Digital Library of Mathematical Functions,
Section 10.32, Integral Representations of Modified Bessel Functions.
\bibitem{dawbaskes} M. S. Daw and M. I. Baskes, ``Embedded-atom method:
Derivation and application to impurities, surfaces, and other defects in
metals,'' \emph{Phys. Rev. B} 29 (1984) 6443--6453.
\bibitem{lu} G. Lu, N. Kioussis, V. V. Bulatov, and E. Kaxiras,
``Generalized-stacking-fault energy surface and dislocation properties of
aluminum,'' \emph{Phys. Rev. B} 62 (2000) 3099--3108.
\end{thebibliography}
\end{document}
