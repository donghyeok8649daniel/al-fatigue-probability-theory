# Active 1D theory index

The authoritative mathematical mainline is now:

1. `MASTER_1D_P_U_THETA_FORMULATION.md` — complete active differential equation set;
2. `MILESTONE25_EXACT_INTEGRAL_REPRESENTATION.md` — exact full-flow, Volterra, characteristic, and first-passage integral representations;
3. `VARIABLE_INDEX_1D_P_U_THETA.md` — symbols and definitions;
4. `MILESTONE24_THETA_HISTORY_STATE.md` — corrected history-bearing $P$–$u$–$\Theta$ interpretation;
5. `CRACK_INITIATION_DEFINITION.md` — kinetic first-passage initiation definition;
6. `theory/normal_lj_chain.py` — closed finite microscopic chain;
7. `theory/normal_lj_distribution_shape.py` — exact smooth density-shape reconstruction;
8. `theory/normal_lj_moment_hierarchy.py` — corrected exact $\Theta$ balance and spacing-coordinate kinetic metric.

## Active chain

$$
\boxed{
\text{LJ chain mechanics}
\to \Phi^q
\to F_M(\lambda,c,t)
\to\{P,u,\Theta\}
\to\{\bar a,\bar U,\text{first passage}\}.
}
$$

The reduced differential description is exact but hierarchical. This does not
prevent an exact integral solution representation: given the finite-chain flow
$\Phi^q$ and an initial full-state measure $\mu_0$, all projected fields are
obtained by push-forward integrals.

## Superseded or non-mainline routes

The following material may remain in the repository for historical,
verification, or future-extension purposes but is not part of the active 1D
paper-level governing model unless explicitly re-justified:

- Boltzmann/Gibbs initial distributions;
- named Gaussian/Weibull life or spacing distributions;
- harmonic/Taylor full-distribution closures;
- Smoluchowski/Fokker--Planck mobility closure and its $J^2/(MP)$ dissipation
  formula;
- registry/slip $s$ as a required coordinate for the normal-only mainline;
- FCC lattice geometry as a required basis of the present reduced chain;
- independent statistical-cell or FEM-element probability products.

## Two corrections that must be preserved

### 1. General $\Theta$ equation

For the actual spatial LJ chain,

$$
\boxed{
D_t\Theta
+2\Theta\partial_\lambda u
+\frac1P\partial_\lambda(PC_3)
=2\Psi,
}
$$

where

$$
\Psi=\operatorname{Cov}(c,\ddot\lambda\mid\lambda).
$$

The zero-right-hand-side equation is conditional on $\Psi=0$ and is not the
general chain identity.

### 2. $\Theta$ is not the complete kinetic energy

$$
E[c^2\mid\lambda]=u^2+\Theta
$$

is exact, but the chain kinetic energy in spacing coordinates is

$$
\boxed{
T=\frac12\boldsymbol c^T\mathbf G_\lambda\boldsymbol c,
\qquad
\mathbf G_\lambda=\mathbf L^T\mathbf L.
}
$$

Therefore exact total kinetic energy also requires cross-spacing rate
correlations.

## Integral solution level

For the deterministic finite-chain trajectories generated from $\Gamma_0$,

$$
\boxed{
P(\lambda,t)
=\frac1M\sum_i\int
\delta[\lambda-\Lambda_i(t;\Gamma_0)]\,\mu_0(d\Gamma_0)
}
$$

and analogous exact projection integrals define $u,\Theta,C_3,\Psi$.
Consequently

$$
\boxed{
\text{non-closure of a three-field PDE}
\neq
\text{absence of an exact integral solution representation}.
}
$$

The specimen survival formula also exists exactly once a physically declared
initial realization measure is supplied:

$$
\boxed{
S_{\rm spec}(t)
=\int
\mathbf 1\left[
\max_i\sup_{s\le t}\Lambda_i(s;\Gamma_0)<\lambda_c
\right]\mu_0(d\Gamma_0).
}
$$

Thus the specimen-scale open issue is primarily the physical construction and
validation of $\mu_0$ and the represented correlation scale, not the absence of
an integral survival equation.

## Remaining open physics

The current mathematics does not hide the remaining blockers:

1. G3 irreversibility is not generated by the conservative baseline;
2. microscopic same-force history dependence has not yet been bridged to
   laboratory Hz-scale fatigue memory;
3. the physical initial/specimen measure $\mu_0$ and correlation scale have not
   yet been derived/calibrated;
4. experimental validation remains required.

These are the next physical problems. They are not reasons to replace the
active $P$–$u$–$\Theta$ mathematics with an assumed PDF or empirical damage
variable.
